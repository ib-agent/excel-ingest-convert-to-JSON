# Copy to .env and adjust as needed

# Django basics
DJANGO_SECRET_KEY=replace-me-for-local-dev
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0
CORS_ALLOW_ALL_ORIGINS=true

# Optional storage integration
# Set to 'true' to always persist artifacts and include storage references in responses
USE_STORAGE_SERVICE=true

# Storage backend selection: 'local' or 's3'
STORAGE_BACKEND=local

# Local storage root path (outside the project, for UI demo)
LOCAL_STORAGE_PATH=/Users/jeffwinner/Projects/DocumentProcessingStorage

# Optional storage settings
# STORAGE_ROOT_PREFIX=
STORAGE_PRESIGN_TTL_SECONDS=3600

# S3 settings (only used when STORAGE_BACKEND=s3)
# S3_BUCKET_NAME=
# AWS_REGION=us-east-1
# AWS_ENDPOINT_URL=

# Web server command
# Dev (hot reload):
CMD=python manage.py runserver 0.0.0.0:8000
# Prod-like (uncomment to use):
# CMD=gunicorn excel_converter.wsgi:application --bind 0.0.0.0:8000 --workers=${WEB_CONCURRENCY:-2} --timeout=120

WEB_CONCURRENCY=2
DJANGO_COLLECTSTATIC=true


